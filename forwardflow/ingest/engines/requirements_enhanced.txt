# Enhanced High-Performance File Copying Engine Dependencies

# Core Python dependencies
pybind11>=2.10.0
numpy>=1.21.0

# Hash libraries
xxhash>=3.0.0
cryptography>=3.4.0

# System monitoring and optimization
psutil>=5.8.0

# Platform-specific dependencies
# Windows
pywin32>=300; sys_platform == "win32"

# macOS
# No additional Python packages required for macOS

# Linux
# No additional Python packages required for Linux

# Development dependencies (optional)
pytest>=6.0.0
pytest-cov>=2.12.0
black>=21.0.0
flake8>=3.9.0
mypy>=0.910

# Build dependencies
setuptools>=60.0.0
wheel>=0.37.0
cmake>=3.16.0

# Optional: For network performance testing
iperf3>=0.1.11

# Optional: For advanced file system operations
# These are typically available as system packages
# - libxxhash-dev (Ubuntu/Debian)
# - xxhash-devel (CentOS/RHEL/Fedora)
# - openssl-devel (CentOS/RHEL/Fedora)
# - libssl-dev (Ubuntu/Debian)

# System Requirements:
# 
# Ubuntu/Debian:
#   sudo apt-get install build-essential cmake libxxhash-dev libssl-dev
# 
# CentOS/RHEL/Fedora:
#   sudo yum install gcc-c++ cmake xxhash-devel openssl-devel
#   # or for newer versions:
#   sudo dnf install gcc-c++ cmake xxhash-devel openssl-devel
# 
# macOS:
#   brew install cmake xxhash openssl
# 
# Windows:
#   - Visual Studio 2019 or later with C++ build tools
#   - CMake 3.16 or later
#   - OpenSSL development libraries
#   - xxHash development libraries
